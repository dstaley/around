import pointDistance from 'geaux-hack/utils/point-distance';

module('pointDistance');

// Replace this with your real tests.
test('it works', function() {
  var result = pointDistance();
  ok(result);
});
