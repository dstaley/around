import { autoLink } from 'geaux-hack/helpers/auto-link';

module('AutoLinkHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = autoLink(42);
  ok(result);
});
